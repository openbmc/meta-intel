[Unit]
Description=Fan Sensor
ConditionFileNotEmpty={bindir}/fansensor
StopWhenUnneeded=false

[Service]
Restart=always
RestartSec=5
ExecStart={bindir}/fansensor

[Install]
WantedBy=multi-user.target
